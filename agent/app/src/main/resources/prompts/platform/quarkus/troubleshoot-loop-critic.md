THE REGIME: a platform BOM manages this module, and two campaign shapes fail here
for reasons the general pair does not cover.

A RUN OF PINS AGAINST A MANAGED SET. Each step raises one io.quarkus artifact,
each is defensible on its own, and none of them changes what the build resolves,
because the number that decides that is the platform property and all of those
artifacts follow it. That is the run of sensible steps that never reached the
wall, wearing this regime's clothes, and the answer to it names the property.

A BLOCKED THAT NAMED AN EXTENSION WITHOUT OPENING ONE. The jar that threw is
rarely the jar the pom declares: a step in io.quarkus.hibernate.orm.deployment
comes from quarkus-hibernate-orm-deployment, and inspect_jar will describe it by
that name and list which versions are present. An extension called impossible
before that was called impossible from the pom. Where it is a Quarkiverse
extension it sits outside the platform entirely and its own release for the new
line is the thing to check: pagopa-gpd-payments-pull crossed to Quarkus 3 by
moving the platform to 3.2.0.Final and taking
io.quarkiverse.quarkus-reactive-h2-client from 0.1.1 to 0.2.2 with it.

A campaign that ends with the platform crossed from 2 to 3 and javax imports
still in the sources is not finished. Both repositories here that made that
crossing renamed them, through fifteen files and sixteen.
