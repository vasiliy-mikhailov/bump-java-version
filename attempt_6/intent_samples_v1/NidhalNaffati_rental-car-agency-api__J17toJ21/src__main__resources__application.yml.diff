diff --git a/src/main/resources/application.yml b/src/main/resources/application.yml
index 507d9c5..9df706c 100644
--- a/src/main/resources/application.yml
+++ b/src/main/resources/application.yml
@@ -1,38 +1,37 @@
 spring:
   datasource:
-    url: jdbc:mysql://localhost:3306/rentalCar?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=False;&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Jakarta&useSSL=false
+    url: jdbc:mysql://localhost:3306/RentalCarDB?createDatabaseIfNotExist=true
     username: root
-    password: "rootroot"
+    password: root
     driver-class-name: com.mysql.cj.jdbc.Driver
   jpa:
     show-sql: true
     hibernate:
-      ddl-auto: update
+      ddl-auto: validate
     properties:
       hibernate:
-        dialect: org.hibernate.dialect.MySQL8Dialect
+        dialect: org.hibernate.dialect.MySQLDialect
+        format_sql: true
     database: mysql
     database-platform: MYSQL
-  thymeleaf:
-    cache: false
-    check-template: true
-    check-template-location: true
+
+  flyway:
     enabled: true
-    encoding: UTF-8
-    mode: HTML
-    #    prefix: classpath:/templates/
-    prefix: file:src/main/resources/templates/
-    #    prefix: ${project.basedir}/src/main/resources/templates/
-    suffix: .html
-    # excluded-view-names:
-    # template-resolver-order:
-    # view-names:
-    servlet:
-      content-type: text/html
+    baseline-on-migrate: true
+    locations: classpath:db/migration
+    schemas: RentalCarDB
+
+
   devtools:
     livereload:
       enabled: true
     add-properties: true
+
   output:
     ansi:
-      enabled: ALWAYS
\ No newline at end of file
+      enabled: ALWAYS
+
+server:
+  error:
+    #this helps to avoid the long trace message.
+    include-stacktrace: never
\ No newline at end of file
