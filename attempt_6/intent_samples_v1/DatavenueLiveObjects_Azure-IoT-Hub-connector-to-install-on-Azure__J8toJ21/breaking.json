{
  "meta": {
    "repo": "DatavenueLiveObjects/Azure-IoT-Hub-connector-to-install-on-Azure",
    "stage": "J8->J21",
    "family": "spring-boot-2",
    "sha_from": "57adcd81ecd95b16ab40c2cae72befc4bf6a0359",
    "sha_to": "3d80f5508fd594c14cc3a70ddc3dad05a679bbec"
  },
  "by_file": {
    "src/main/java/com/orange/lo/sample/lo2iothub/azure/IoTDeviceProvider.java": [
      {
        "kind": "migrate_azure_sdk_api",
        "general_idea": "Replace deprecated `DeviceTwin` and `RegistryManager` classes with the newer `TwinClient` and `RegistryClient` from the updated Azure IoT SDK.",
        "human_impl": "registryManager.getDevice(deviceId) -> registryClient.getDevice(deviceId)",
        "bucket": "breaking"
      },
      {
        "kind": "update_device_creation_api",
        "general_idea": "Adapt device creation to use the new `Device` constructor and `RegistryClient.addDevice` method instead of the static factory and old manager.",
        "human_impl": "Device.createFromId(...) -> new Device(deviceId); registryManager.addDevice -> registryClient.addDevice",
        "bucket": "breaking"
      },
      {
        "kind": "update_twin_query_api",
        "general_idea": "Migrate device twin querying from the old `SqlQuery` and `DeviceTwin` iteration pattern to the new `TwinClient.query` and `TwinQueryResponse` pattern.",
        "human_impl": "deviceTwin.queryTwin(sqlQuery) -> twinClient.query(selectingQuery)",
        "bucket": "breaking"
      },
      {
        "kind": "update_twin_patch_api",
        "general_idea": "Update the logic for setting device tags to use the new `Twin` object and `TwinClient.patch` method.",
        "human_impl": "deviceTwin.updateTwin(deviceTwinDevice) -> twinClient.patch(twin)",
        "bucket": "breaking"
      }
    ],
    "src/main/java/com/orange/lo/sample/lo2iothub/lo/LoCommandSender.java": [
      {
        "kind": "replace_rest_with_sdk",
        "general_idea": "Replace manual HTTP REST calls with a dedicated LiveObjects SDK adapter for command sending.",
        "human_impl": "Removed RestTemplate and replaced with LoAdapter.sendCommand()",
        "bucket": "breaking"
      },
      {
        "kind": "change_method_signature",
        "general_idea": "Change the send method to return an IotHubMessageResult instead of void to indicate success or rejection.",
        "human_impl": "Changed return type from void to IotHubMessageResult and added return statements",
        "bucket": "breaking"
      },
      {
        "kind": "add_json_parsing",
        "general_idea": "Parse the incoming command string into a structured CommandAddRequest object using Jackson.",
        "human_impl": "Added ObjectMapper dependency and objectMapper.readValue(command, CommandAddRequest.class)",
        "bucket": "breaking"
      }
    ],
    "pom.xml": [
      {
        "kind": "bump_java_version",
        "general_idea": "Update the target Java version property from 1.8 to 21 to align with the new JDK requirement.",
        "human_impl": "<java.version>21</java.version>",
        "bucket": "breaking"
      },
      {
        "kind": "bump_spring_boot_parent",
        "general_idea": "Upgrade the Spring Boot parent POM from 2.1.8.RELEASE to 3.2.3, which requires Java 17+ and introduces breaking API changes.",
        "human_impl": "<version>3.2.3</version>",
        "bucket": "breaking"
      }
    ]
  }
}