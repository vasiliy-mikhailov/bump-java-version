diff --git a/src/main/java/com/fractalshot/xmlsplit/Writer.java b/src/main/java/com/fractalshot/xmlsplit/Writer.java
index 299950c..78f264f 100644
--- a/src/main/java/com/fractalshot/xmlsplit/Writer.java
+++ b/src/main/java/com/fractalshot/xmlsplit/Writer.java
@@ -1,6 +1,7 @@
 package com.fractalshot.xmlsplit;
 
 import java.io.*;
+import java.nio.charset.StandardCharsets;
 import java.util.zip.ZipEntry;
 import java.util.zip.ZipOutputStream;
 
@@ -9,12 +10,13 @@ public class Writer {
     public final int index;
     public long writtenFile = 0;
     private final BufferedWriter bufferedWriter;
+    private final OutputStream outputStream;
     private ZipOutputStream zipOutputStream = null;
 
     public Writer(String basename, String extension, String zipEntryName, int index) throws IOException {
         this.index = index;
         String filename = basename + "_" + index + "." + extension;
-        OutputStream outputStream;
+
         if (extension.equals(ZIP_EXTENSION)) {
             zipOutputStream = new ZipOutputStream(new FileOutputStream(filename));
             zipOutputStream.putNextEntry(new ZipEntry(zipEntryName));
@@ -22,7 +24,7 @@ public class Writer {
         } else {
             outputStream = new FileOutputStream(filename);
         }
-        bufferedWriter = new BufferedWriter(new OutputStreamWriter(outputStream), 1000000);
+        bufferedWriter = new BufferedWriter(new OutputStreamWriter(outputStream, StandardCharsets.UTF_8), 1000000);
         write(HandlerElement.header.toString());
     }
 
